/* OPERADORES DE COMPARAÇÃO
*
* " > " Maior que
* " >= " Maior OU igual
* " < " Menor que
* " <=  " Menos OU igual
* " == " Igualdade (valor)  -> NÂO RECOMENDADO
* " === " Igualdade estrita ( valor e tipo)
* " != " Diferente (valor)  -> NÂO RECOMENDADO
* " !== " Diferente estrito (valor e tipo)
*
*/

/* OPORADORES LÓGICOS
*
* && -> AND -> E
* || -> OR  -> ou
* !  -> NOT -> NÂO
*
*/

/* if, else if, else
*
*   if pode ser usado sozinho 
*   Else só pode ser utilizado caso aja um " if " antes e
pode existir apenas um por condição no final desta.
*   Else if pode ser criado varias vezes
*
*/
const hora = 12;

if (hora > 0 && hora < 13) {
    console.log('');
}